from scapy.all import *

# List available interfaces and their names
# Run this code to get a list of available network interfaces along with their names
print(ifaces)

# After running the code, look for the Wi-Fi or Ethernet interface name in the printed list
# Replace 'wifi_interface' with the actual interface name in the other programs

# Example usage for Wi-Fi interface:
# wifi_interface = 'Wi-Fi Interface Name'  # Replace 'Wi-Fi Interface Name' with the actual Wi-Fi interface name from the list